{
  "name": "@robo-journey/assistant",
  "version": "0.0.0",
  "private": true,
  "description": "The in-app assistant. Answers questions about the circuit on screen. Server-side only: the API key never reaches the browser.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc --build" },
  "dependencies": {
    "@google/genai": "^2.17.1",
    "@robo-journey/parts": "*",
    "zod": "^4.4.3"
  }
}
