node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.vite/
coverage/

# Account database. Contains password hashes and session tokens — never commit it.
*.db
*.db-wal
*.db-shm

# Site files the installer writes for an existing proxy. Machine-specific.
deploy/generated/
